<!DOCTYPE html>
<html lang="en-us">
    <head>
        <meta charset="utf-8">
        <title>
        {% if page.title %}
          {{ page.title }} &ndash;
        {% endif %}
        {{ site.name }}
        </title>
        <meta name="author" content="{{ site.name }}" />
        <meta name="description" content="{{ site.description }}" />
        <link rel="stylesheet" href="/css/simple.css" type="text/css" />
    </head>
    <header>
      <p><a href="/index.html">Info</a> / <a href=".">Projects</a> / <a href=".">Experiments</a></p>
    </header>
    <body>
    {{ content }}
    </body>
</html>
